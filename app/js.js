// Simulate generating a large number of lines
function generateLines() {
    for (let i = 0; i < 1e9; i++) { // Adjust loop for practicality
        console.log(`Line ${i}: console.log('Hello World');`);
    }
}

// Uncomment to run (be cautious with memory usage!)
// generateLines();
